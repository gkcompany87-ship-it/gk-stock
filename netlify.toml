[build]
  command = "pnpm --filter @as-tino/ui build && pnpm --filter @as-tino/shared build && pnpm --filter @as-tino/web build"
  publish = "apps/web/.next"

[build.environment]
  NODE_VERSION = "24"
  PNPM_VERSION = "10.28.2"
